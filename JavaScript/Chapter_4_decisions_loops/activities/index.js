const randomNumber = Math.floor(Math.random() * 10);

/* Activity 1
    Create a function called randomWinner that takes a random number.
    If the number is greater than 6, alert ('You Win').
    Else, alert ('Sorry, try again');
*/

const colors = ['red', 'green', 'blue', 'yellow', 'purple', 'orange'];

/* Activity 2
    Create a for loop that loops through the array of colors
    and logs them to the console.
*/

